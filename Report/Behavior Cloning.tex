\documentclass{article}
\usepackage[pdftex,active,tightpage]{preview}
\setlength\PreviewBorder{2mm}

\usepackage[utf8]{inputenc} % this is needed for umlauts
\usepackage[ngerman]{babel} % this is needed for umlauts
\usepackage[T1]{fontenc}    % this is needed for correct output of umlauts in pdf
\usepackage{amssymb,amsmath,amsfonts} % nice math rendering
\usepackage{braket} % needed for \Set
\usepackage{caption}
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}
\usepackage{algorithm,algpseudocode}



\DeclareCaptionFormat{myformat}{#3}
\captionsetup[algorithm]{format=myformat}
\DeclareMathOperator*{\argmax}{arg\,max}

\newcommand{\vars}{\texttt}


\begin{document}
\begin{preview}
    \begin{algorithm}[H]
	\caption{Algorithm:Deep Q Learning algorithm}
	\begin{algorithmic}
	\Function{Behavior Cloning}{$\vars{M as mentor, E as enviroment}$}
		\State $\vars{S}$ $\gets$ $\vars{E}$.Get State()
		\State $\vars{A}$ $\gets$ $\vars{M}$.Get Action()
		\State $\vars{R}$ $\gets$ $\vars{E}$.Do Action($\vars{A}$)
		\State $\vars{S$^\prime$}$ $\gets$ $\vars{E}$.Get State()
		\State Observe($\vars{S, A, R, S$^\prime$}$)
	\EndFunction
	\end{algorithmic}
    \end{algorithm}
\end{preview}
\end{document}